docker.file = ["src/test/resources/docker-redis-latest.yml"]

mixing {
    jdbc {
        mixing {
            database = "disabled"
        }
        clickhouse {
            database = "disabled"
        }
    }
}

mongo.databases.mixing.hosts = ""
elasticsearch.hosts = ""
