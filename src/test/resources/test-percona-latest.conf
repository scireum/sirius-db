docker.file = ["src/test/resources/docker-percona-latest.yml"]

mixing {
    jdbc {
        clickhouse {
            database = "disabled"
        }
    }
}

mongo.databases.mixing.hosts = ""
elasticsearch.hosts = ""
redis.pools.system.host = "localhost"
