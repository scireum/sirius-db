docker.file = "src/test/resources/docker-percona-latest.yml"

mixing {
    jdbc {
        clickhouse {
            database = "disabled"
        }
    }
}

mongo.hosts = ""
elasticsearch.hosts = ""
redis.host = ""

