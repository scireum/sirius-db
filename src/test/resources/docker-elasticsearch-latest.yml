version: "2"

services:
  elasticsearch:
    image: elasticsearch:latest
    ports:
    - "9200"
    environment:
    - xpack.security.enabled=false
    - script.inline=true
    - ES_JAVA_OPTS=-Xms32M -Xms128M
    hostname: es
