docker.file = "src/test/resources/docker-mongo-latest.yml"

mixing {
    jdbc {
        mixing {
            database = "disabled"
        }
        clickhouse {
            database = "disabled"
        }
    }
}

mongo {
    databases.mixing {
        hosts: "localhost"
        db: "test"
    }
}

elasticsearch.hosts = ""
redis.pools.system.host = "localhost"
