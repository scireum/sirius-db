docker.file = "src/test/resources/docker-postgres-latest.yml"

jdbc {

    database {
        test {
            profile = "postgres"
            user = "postgres"
            password = "root"
            database = "test"
        }
    }

}

#mixing.updateSchema = false
#mixing.database = "disabled"
