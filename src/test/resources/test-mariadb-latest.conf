docker.file = "src/test/resources/docker-mariadb-latest.yml"

jdbc {

    database {
        test {
            driver = "org.mariadb.jdbc.Driver"
            profile = "mysql"
            user = "root"
            password = "root"
            database = "test"
        }
    }

}
