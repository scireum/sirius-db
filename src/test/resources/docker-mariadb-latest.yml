version: "3"

services:
  mariadb:
    image: mariadb:latest
    ports:
      - "3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
    hostname: mariadb
    volumes:
      - ./docker/mysql:/docker-entrypoint-initdb.d/:ro
