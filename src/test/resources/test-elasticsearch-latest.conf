docker.file = "src/test/resources/docker-elasticsearch-latest.yml"

mixing {
    jdbc {
        mixing {
            database = "disabled"
        }
        clickhouse {
            database = "disabled"
        }
    }
}

mongo {
    hosts = ""
}
